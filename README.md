
# Run server
- cd server 
- npm install
- npm start

# Run webpage
- cd webpage
- npm install
- npm start


# Routes
- `/` - leaderboard
- `/admin` - admin panel


# Clear data
- Delete `laps.json` and `users.json` from `./server`
- Restart server
